#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: integer to compute its abs value
 *
 * Return: the abs value of the int
 */
int _abs(int n)
{
	if (n <= 0)
		return (-n);
	return (n);
}
