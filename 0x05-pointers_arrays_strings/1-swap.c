#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: param of first int
 * @b: param of second int
 */
void swap_int(int *a, int *b)
{
	int *p = &a;

	int temp = *p

	a = b;
	b = temp;
}
